package simulator

import system.elevator.ElevatorController
import system.enums.{Down, Up}

object Main {
  def main(args: Array[String]): Unit = {
//    val elevatorCount = 16
//    val controller = new ElevatorController(elevatorCount)
//    for (i <- Range(0, elevatorCount)) {
//      controller.update(i, i, if (i % 2 == 0) Up else Down)
//    }
//    controller.step()
//    println(controller.status())
//    controller.step()
//    controller.step()
//    controller.step()
//    println(controller.status())
    //    val elevator = new Elevator(1)

  }
}
